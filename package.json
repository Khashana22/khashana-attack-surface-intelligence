{
  "name": "khashana-attack-surface-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "A local-only, simulated attack surface intelligence portfolio platform.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "lint": "node --check server.js && node --check public/app.js"
  },
  "engines": { "node": ">=20" }
}
