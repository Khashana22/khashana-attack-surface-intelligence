{
  "name": "khashana-attack-surface-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "External attack surface intelligence and vulnerability prioritization platform for controlled security assessments.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "lint": "node --check server.js && node --check api/index.js && node --check public/app.js"
  },
  "engines": { "node": ">=20" }
}
